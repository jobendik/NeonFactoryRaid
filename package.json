{
  "name": "neon-factory-raid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Neon Factory Raid - top-down auto-shooter + idle factory + extraction risk",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "phaser": "^3.87.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
